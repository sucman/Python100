# -*- coding:utf-8 -*-
'''
按相反的顺序输出列表的值。

当s<0时，i缺省时，默认为-1. j缺省时，默认为-len(a)-1
所以a[::-1]相当于 a[-1:-len(a)-1:-1]，也就是从最后一个元素到第一个元素复制一遍
'''

# 正序倒序

a = ['one', 'two', 'three']
for i in a[::-1]:
    print i
